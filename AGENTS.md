# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

# System orientation

Read `PROJECT_MEMO.md` (repo root) before working on a feature you don't already have context on — it's a system-wide reference covering both this repo and `bsm_system` (the web app + the shared backend this app calls): roles/permissions model, domain entities, feature-by-feature web/mobile parity map, and current UI conventions.
